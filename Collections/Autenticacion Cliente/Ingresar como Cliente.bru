meta {
  name: Ingresar como Cliente
  type: http
  seq: 1
}

post {
  url: {{base_url}}/usuarios/login
  body: json
  auth: none
}

body:json {
  {
    "email": "alejandro.dev@optiturno.com",
    "password": "PasswordSeguro123"
  }
}

tests {
  script: const data = res.getBody();
  bru.setVar("token_cliente", data.token);
  bru.setVar("usuario_real_id", data.usuario.id);
}